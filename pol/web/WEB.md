# Веб-приложение «Задачи по дням»

Веб-версия открывается в браузере на **ПК и телефоне**. Данные можно хранить **локально** (localStorage) или в **облаке** (Firebase) — тогда одни и те же задачи и слова видны на всех устройствах (ПК, телефон, любая сеть).

---

## Как открыть на телефоне через Wi‑Fi (с сохранением)

### Шаг 1: Запустить сервер на компьютере

1. Подключите **ПК и телефон к одной Wi‑Fi сети**.
2. На ПК откройте папку **`web`** (в проекте: `d:\pol\web`).
3. Дважды нажмите на файл **`start_server.bat`**.
4. Появится чёрное окно. В нём будет строка вида:
   ```text
   На телефоне в браузере откройте адрес:
   http://192.168.1.XXX:8080
   ```
   Запомните или скопируйте этот адрес (числа у вас могут быть другие). **Окно не закрывайте** — пока оно открыто, приложение доступно с телефона.

Если при запуске пишет «Python не найден» — установите Python с [python.org](https://www.python.org/downloads/) (при установке отметьте «Add Python to PATH») и снова запустите `start_server.bat`.

### Шаг 2: Открыть приложение на телефоне

1. На телефоне откройте **браузер** (Chrome, Safari, Яндекс и т.п.).
2. В адресной строке введите адрес из окна на ПК, например: **`http://192.168.1.XXX:8080`** (подставьте свой IP и порт 8080).
3. Откроется приложение «Задачи по дням».

### Шаг 3: Сохранение данных

**Вариант А — данные только на телефоне**

- Всё, что вы делаете (задачи, слова, уровень), **сохраняется в браузере телефона** автоматически.
- Данные будут только на этом телефоне. На ПК — свои данные (если открывать по `http://localhost:8080`).

**Вариант Б — одни и те же данные на ПК и телефоне (облако)**

1. Настройте **Firebase** (один раз) — см. раздел **«Облако»** ниже.
2. В приложении появится надпись **«☁ Облако вкл»**.
3. Тогда и на ПК, и на телефоне (по тому же адресу или по ссылке из интернета) будут **одни и те же** задачи и слова, с сохранением в облаке.

**Вариант В — перенос данных вручную**

- На ПК: нажмите **«📥 Скачать данные»** — сохранится файл.
- Перешлите его себе (Telegram, почта, облако) и на телефоне откройте приложение, нажмите **«📤 Загрузить данные»** и выберите этот файл. Данные с ПК появятся на телефоне.

---

## Облако (одни данные на ПК и телефоне через Wi‑Fi или интернет)

1. Зайдите на [Firebase Console](https://console.firebase.google.com/), создайте проект.
2. В проекте: **Realtime Database** → создать базу (режим теста или выберите регион).
3. В правилах доступа для теста можно временно разрешить чтение и запись: `"rules": { ".read": true, ".write": true }` (потом лучше ограничить).
4. В настройках проекта (**Project settings** → ваше веб-приложение) скопируйте конфиг (apiKey, authDomain, databaseURL и т.д.).
5. В папке `web` откройте **firebase-config.js**, вставьте конфиг в `window.firebaseConfig = { ... };` (раскомментируйте блок и подставьте свои значения).
6. Откройте приложение на ПК и на телефоне (по ссылке или через start_server.bat). Вверху будет **«☁ Облако вкл»** — задачи, слова и прогресс синхронизируются между всеми устройствами в реальном времени.

Если конфиг не задан, приложение работает только **локально** (надпись «☁ Локально»).

## Как открыть локально

1. Откройте папку `web` в проекте.
2. Запустите любой простой веб-сервер из этой папки, например:
   - **Python 3:** `python -m http.server 8080`
   - **Node.js (npx):** `npx serve . -p 8080`
   - Или дважды кликните по `index.html` (файл откроется в браузере по адресу `file:///...`; в этом режиме часть возможностей может быть ограничена).

3. В браузере откройте: **http://localhost:8080** (или тот порт, который указали).

На телефоне в той же Wi‑Fi сети: введите в браузере адрес **http://IP_вашего_ПК:8080** (IP можно посмотреть в настройках сети на компьютере).

## Как выложить в интернет (чтобы открывать с любого устройства)

Тогда приложение будет доступно по ссылке с любого ПК и телефона.

### Вариант 1: GitHub Pages (бесплатно)

1. Создайте репозиторий на [GitHub](https://github.com).
2. Загрузите в него содержимое папки `web` (файлы `index.html`, `style.css`, `app.js` — в корне репозитория или в папке `web`).
3. В настройках репозитория: **Settings → Pages → Source** выберите ветку (например, `main`) и папку с сайтом (корень или `web`).
4. Через пару минут сайт будет доступен по адресу вида:  
   `https://ваш-логин.github.io/имя-репозитория/`

### Вариант 2: Netlify / Vercel (бесплатно)

1. Зарегистрируйтесь на [Netlify](https://netlify.com) или [Vercel](https://vercel.com).
2. Создайте новый сайт и укажите папку `web` (или загрузите её).
3. Сайт получит адрес вида `https://ваш-проект.netlify.app`.

### Вариант 3: свой хостинг

Скопируйте папку `web` на любой хостинг с поддержкой статических сайтов (HTML/CSS/JS). Достаточно, чтобы по URL открывался `index.html`.

## Синхронизация между компьютером и телефоном

Чтобы одни и те же данные были и на ПК, и на телефоне:

1. **На компьютере** (или на телефоне): нажмите **«📥 Скачать данные»**. В папку загрузок сохранится файл `pol_backup_ГГГГ-ММ-ДД.json` со всеми задачами, словами, уровнем/XP и изображениями молитв.
2. **Перенесите файл** на другое устройство любым способом:
   - отправьте себе в Telegram / WhatsApp / по почте и откройте на втором устройстве;
   - положите файл в облако (Google Drive, Яндекс.Диск, Dropbox) и откройте или скачайте на втором устройстве.
3. **На втором устройстве**: откройте веб-приложение, нажмите **«📤 Загрузить данные»**, выберите скачанный файл. Данные заменятся на те, что были в файле.

Так можно:
- сохранить состояние с ПК и загрузить его на телефон;
- сохранить состояние с телефона и загрузить на ПК;
- делать резервные копии (периодически нажимать «Скачать данные» и хранить файл).

**Важно:** при «Загрузить данные» текущие данные в этом браузере **заменяются** на данные из файла. Если нужно сохранить и старые, и новые данные, сначала скачайте текущие данные, потом загрузите файл с другого устройства и при необходимости объедините вручную (или оставьте один вариант).

## Что хранится в браузере

- Задачи (название, описание, срок, приоритет, категория, статус).
- Слова по урокам английского (A1.1 … C1.50).
- Уровень, XP и серия дней (геймификация).
- Изображения к главам молитвы (в виде base64 в localStorage).

Данные привязаны к домену и браузеру. Экспорт/импорт позволяет переносить их между устройствами (см. выше).

## Адаптивность

Вёрстка подстраивается под экран: на узком экране (телефон) вкладки и блоки перестраиваются для удобного нажатия пальцем.
